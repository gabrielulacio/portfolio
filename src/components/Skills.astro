---
import SectionHeader from './ui/SectionHeader.astro';
import { Icon } from 'astro-icon/components';

interface Skill {
  name: string;
  icon: string;
}

interface SkillGroup {
  category: string;
  skills: Skill[];
}

const skillGroups: SkillGroup[] = [
  {
    category: 'Lenguajes',
    skills: [
      { name: 'Python',     icon: 'simple-icons:python'     },
      { name: 'SQL',        icon: 'mdi:database'            },
      { name: 'JavaScript', icon: 'simple-icons:javascript' },
      { name: 'C++',        icon: 'simple-icons:cplusplus'  },
      { name: 'Java',       icon: 'mdi:language-java'        },
    ],
  },
  {
    category: 'Frontend & UI',
    skills: [
      { name: 'React.js',    icon: 'simple-icons:react'        },
      { name: 'Vue.js',      icon: 'simple-icons:vuedotjs'     },
      { name: 'Astro',       icon: 'simple-icons:astro'        },
      { name: 'Tailwind CSS',icon: 'simple-icons:tailwindcss'  },
      { name: 'Figma',       icon: 'simple-icons:figma'        },
    ],
  },
  {
    category: 'Bases de Datos',
    skills: [
      { name: 'PostgreSQL', icon: 'simple-icons:postgresql' },
      { name: 'MySQL',      icon: 'simple-icons:mysql'      },
      { name: 'Oracle DB',  icon: 'simple-icons:oracle'     },
      { name: 'MongoDB',    icon: 'simple-icons:mongodb'    },
    ],
  },
  {
    category: 'Herramientas',
    skills: [
      { name: 'Git / GitHub', icon: 'simple-icons:github'    },
      { name: 'HTML5',        icon: 'simple-icons:html5'     },
      { name: 'CSS3',         icon: 'simple-icons:css3'      },
      { name: 'Prompt Eng.',  icon: 'mdi:robot-outline'      },
    ],
  },
];
---

<section
  id="habilidades"
  class="mx-auto max-w-6xl px-6 py-24"
  style="border-top: 1px solid var(--color-border);"
  aria-labelledby="skills-heading"
>
  <SectionHeader label="// stack técnico" title="Habilidades" />

  <div
    class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4"
  >
    {skillGroups.map(({ category, skills }, gi) => (
      <div
        class="sr flex flex-col gap-4"
        style={`transition-delay: ${gi * 150}ms;`}
      >
        <h3
          class="text-xs font-medium uppercase tracking-[0.12em] pb-3"
          style="
            color: var(--color-text-muted);
            font-family: var(--font-display);
            border-bottom: 1px solid var(--color-border);
          "
        >
          {category}
        </h3>

        <ul class="flex flex-col gap-1" role="list">
          {skills.map(({ name, icon }, si) => (
            <li
              class="skill-item sr flex items-center gap-3 px-3 py-2 rounded-lg transition-all duration-200"
              style={`transition-delay: ${gi * 150 + si * 60}ms;`}
            >
              <Icon
                name={icon}
                class="skill-icon w-4 h-4 shrink-0 transition-all duration-200"
                style="color: var(--color-text-muted);"
                aria-hidden="true"
              />
              <span
                class="skill-name text-sm transition-all duration-200"
                style="color: var(--color-text-muted);"
              >
                {name}
              </span>
            </li>
          ))}
        </ul>
      </div>
    ))}
  </div>
</section>

<style>
  .skill-item:hover {
    background: color-mix(in srgb, var(--color-accent) 10%, transparent);
  }

  .skill-item:hover .skill-icon {
    color: var(--color-accent) !important;
    filter: drop-shadow(0 0 4px color-mix(in srgb, var(--color-accent) 60%, transparent));
    transform: scale(1.2);
  }

  .skill-item:hover .skill-name {
    color: var(--color-text) !important;
    font-weight: 500;
  }
</style>

<script>
  import { initScrollReveal } from '../scripts/scroll-reveal.ts';
  initScrollReveal();
</script>
